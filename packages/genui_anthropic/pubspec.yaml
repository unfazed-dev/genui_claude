name: genui_anthropic
description: GenUI Anthropic Flutter package for the anthropic_genui monorepo.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  # Reference to sibling package using path dependency
  anthropic_a2ui:
    path: ../anthropic_a2ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
